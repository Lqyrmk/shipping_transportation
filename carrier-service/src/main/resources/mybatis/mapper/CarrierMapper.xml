<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lqyrmk.transportation.mapper.CarrierMapper">

    <!--Carrier getOrderByOrderIdByStep2(@Param("carrierId") Long carrierId);-->
    <select id="getOrderByOrderIdByStep2" resultType="Carrier">
        select * from carrier where carrier_id = #{carrierId}
    </select>

</mapper>
